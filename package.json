{
  "name": "hexoboards",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Wrangler app for hexoboards.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env=\"\"",
    "deploy:staging": "wrangler deploy --env staging",
    "check": "node --check src/index.js",
    "test": "node scripts/test-share.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "4.80.0"
  }
}
